import type { Meta } from '../../../types/type'

export const meta: Meta = {
	author: 'Rich Guy',
	title: 'Whatever',
}

export const demoCode: string = `import { Button } from '@mantine/core'

function Demo() {
return <Button>Hello</Button>
}`
